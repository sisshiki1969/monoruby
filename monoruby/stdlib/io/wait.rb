# frozen_string_literal: true
#
# `io/wait` stub for monoruby.
#
# `IO#wait`, `IO#wait_readable`, `IO#wait_writable`, and
# `IO#wait_priority` are implemented as built-in methods directly on
# the IO class, so this file exists only so that `require "io/wait"`
# succeeds without raising `LoadError`.
