[package]
name = "libxml2-src"
version = "2.13.8"
edition = "2024"
description = "libxml2 2.13.8 with nokogiri's patches, built from the vendored source and linked statically; the FFI monoruby's Nokogiri uses"
license = "MIT"
links = "xml2"
build = "build.rs"
publish = false

[dependencies]

[build-dependencies]
cc = "1.0"
