#!/bin/sh

RUSTFLAGS=-Cforce-frame-pointers=yes cargo install --features perf --path monoruby
perf record -F 500 -g -- monoruby $@
perf script > perf
../FlameGraph/stackcollapse-perf.pl perf | ../FlameGraph/flamegraph.pl > out.svg
rm perf.data*
